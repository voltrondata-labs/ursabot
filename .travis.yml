language: python
services:
  - docker
python:
  - 3.6
cache:
  - pip
script:
  - pip install -r requirements.txt pytest>=3.9 flake8

  # run linter
  - flake8

  # run the worker tests (docker utilities for now)
  - pytest -v workers

  # check the buildbot configuration
  - pip install buildbot
  - buildbot checkconfig
